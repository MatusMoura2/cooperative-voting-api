services:
  db:
    image: postgres:15-alpine
    container_name: cpf-verification-db
    environment:
      POSTGRES_DB: cpf_verification
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
